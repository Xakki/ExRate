framework:
    test: true
    session:
        storage_factory_id: session.storage.factory.mock_file
    php_errors:
        log: true
        throw: true

    messenger:
        transports:
            sync: 'sync://'
        routing:
            'App\Message\FetchRateMessage': sync
            'Symfony\Component\Scheduler\RecurringMessage': sync

